namespace _7thHomework.Task2
{
    using System;
    using SetNamespace;
    using ListNamespace;

    class Program
    {
        static void Main(string[] args)
        {
            var set = new Set<int>();
            var list = new List<int>();

            foreach(int element in list)
            {
                
            }

            /* var secondSet = new Set<int>();
             var set = new Set<int>(); 

             set.Add(1);
             set.Add(2);

             set.Union(secondSet);

             var resultSet = new Set<int>();
             resultSet.Add(1);
             resultSet.Add(2);*/
        }
    }
}
